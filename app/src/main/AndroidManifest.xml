<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pdbsflhr22.shareride">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.pdbsflhr22.shareride.SplashActivity" />
        <activity android:name="com.example.pdbsflhr22.shareride.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.pdbsflhr22.shareride.BeforeLogin" />
        <activity android:name="com.example.pdbsflhr22.shareride.LoginActivity" />
        <activity android:name="com.example.pdbsflhr22.shareride.SignupActivity" />
        <activity
            android:name="com.example.pdbsflhr22.shareride.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.pdbsflhr22.shareride.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="com.example.pdbsflhr22.shareride.ConfirmRide" />
        <activity android:name="com.example.pdbsflhr22.shareride.RideActivity" />
        <activity android:name="com.example.pdbsflhr22.shareride.Recycler" />
        <activity android:name="com.example.pdbsflhr22.shareride.MyRide" />
        <activity android:name="com.example.pdbsflhr22.shareride.Schedule" />
        <activity android:name="com.example.pdbsflhr22.shareride.History" />
        <activity android:name="com.example.pdbsflhr22.shareride.Profile"/>
        <activity android:name="com.example.pdbsflhr22.shareride.AboutUs"/>
        <activity android:name="com.example.pdbsflhr22.shareride.ActivityEdit_profile"/>
        <activity android:name="com.example.pdbsflhr22.shareride.SettingsActivity" />

    </application>

</manifest>